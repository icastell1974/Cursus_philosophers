{
    "version":"2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "pwd > p"
            //"make -C {workspaceFolder}/ re"
        }
    ]
}
